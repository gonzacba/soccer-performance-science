name: soccer_performance
version: '1.0.0'
config-version: 2

profile: soccer_performance

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  soccer_performance:
    base:
      +materialized: view
    staging:
      +materialized: view
    marts:
      +materialized: table